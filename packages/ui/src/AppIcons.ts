import type { IconName } from "./components/Icon";

export const AppIcon = {
  Home: "home" as IconName,
  HomeFilled: "home-filled" as IconName,
  Procesos: "git-network" as IconName,
  ProcesosFilled: "git-network" as IconName,
  Ot: "construct" as IconName,
  OtFilled: "construct" as IconName,
  Calendario: "calendar" as IconName,
  CalendarioFilled: "calendar-filled" as IconName,
  Reportes: "bar-chart" as IconName,
  ReportesFilled: "bar-chart-filled" as IconName,
  Configuracion: "settings" as IconName,
  ConfiguracionFilled: "settings-filled" as IconName,
  Inspeccion: "clipboard" as IconName,
  InspeccionFilled: "clipboard-filled" as IconName,
  Logout: "log-out" as IconName,
} as const;
